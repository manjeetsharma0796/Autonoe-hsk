// data.ts - types only. All token data now comes from /api/symbols via getSymbols().
// See MarketsTable.tsx and MoversStrip.tsx for live-fetching hooks.

export type { TokenInfo } from '../../lib/api';
