{
  "name": "@autonoe/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Autonoe on-chain layer: mUSD, WHSK, UniswapV2-style AMM, PriceOracle + SyntheticExchange (hybrid), DecisionLog",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "typecheck": "tsc --noEmit",
    "deploy:hashkey": "hardhat run scripts/deploy.ts --network hashkeyTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.1.0"
  }
}
